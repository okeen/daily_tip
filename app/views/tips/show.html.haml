%p
  %b Title:
  = @tip.title
%p
  %b Content:
  = @tip.content

%p
  %b Author:
  = @tip.author.email

= tip_tag_list_for(@tip)

= tip_links_for(@tip)

= link_to(t("edit"), edit_tip_path(@tip)) if @tip.author == current_user

= link_to t("back"), tips_path
